services:
  - type: web
    name: agente-financeiro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_web:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
